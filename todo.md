# 智能聚合功能任务清单

## Phase 1: 实现连续日期自动聚合逻辑

### 核心需求
- [ ] 当用户在日历上点击标记新日期时,检查是否与现有单日记录连续
- [ ] 如果连续,自动合并为一个时间段
- [ ] 如果不连续,创建新的单日记录
- [ ] 取消标记时,如果是时间段的一部分,需要拆分时间段

### 实现细节

#### 1. 连续性判断逻辑
- 检查新标记日期的前一天或后一天是否有单日记录
- 检查新标记日期是否与现有时间段相邻(startDate-1 或 endDate+1)
- 支持多个方向的合并(前后都有记录时合并为一个大时间段)

#### 2. 合并策略
- **场景1**: 新日期与单日记录相邻 → 合并为2天时间段
- **场景2**: 新日期与时间段相邻 → 扩展时间段
- **场景3**: 新日期连接两个时间段 → 合并为一个大时间段
- **场景4**: 新日期孤立 → 创建新单日记录

#### 3. 拆分策略(取消标记时)
- **场景1**: 取消单日记录 → 直接删除
- **场景2**: 取消时间段的起始日 → 缩短时间段(startDate+1)
- **场景3**: 取消时间段的结束日 → 缩短时间段(endDate-1)
- **场景4**: 取消时间段的中间日 → 拆分为两个时间段

#### 4. 数据结构调整
- 需要修改 `toggleDeparture` 函数
- 添加辅助函数:
  - `findAdjacentPeriods(date)`: 查找与指定日期相邻的时间段
  - `mergePeriods(periods)`: 合并多个时间段
  - `splitPeriod(period, date)`: 拆分时间段

#### 5. 日期计算工具
- 添加日期加减天数的工具函数
- 添加计算两个日期之间天数的函数
- 添加判断日期是否连续的函数

### 文件修改清单
- [ ] `/home/ubuntu/uk-ilr-tracker/client/src/lib/dateUtils.ts` - 创建日期工具函数
- [ ] `/home/ubuntu/uk-ilr-tracker/client/src/contexts/AppContext.tsx` - 修改 toggleDeparture 函数

## Phase 2: 测试并交付更新版本

### 测试场景
- [ ] 测试标记连续日期的自动合并
- [ ] 测试取消标记时的拆分逻辑
- [ ] 测试边界情况(跨月、跨年)
- [ ] 测试数据持久化

### 交付清单
- [ ] 保存检查点
- [ ] 向用户交付更新版本
